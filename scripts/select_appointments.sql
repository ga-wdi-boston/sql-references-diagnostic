-- retrieve appointment information

SELECT





-- Template
-- -- get information using addresses join table
-- SELECT p.given_name, p.surname
--     FROM people p
--     INNER JOIN addresses a on a.person_id = p.id
--     INNER JOIN cities c on a.skill_id = c.id
--       where c.name = 'Chicago'
-- ;
--
